name: Trigger Release

on:
  release:
    types: [published]
    
permissions:
  contents: write
  
jobs:
  trigger-release:
    runs-on: ubuntu-latest

    steps:
      - name: Print hello
        run: echo "HELLOOO"
      - name: Trigger Release in Target Repo
        uses: peter-evans/repository-dispatch@v2
        env:
          token: ${{ secrets.PAT }}
        with:
          token: ${{ secrets.NEW }}  # Personal Access Token or Repo Token with appropriate permissions
          repository: ankatiyar/kedro-starters
          event-type: release-triggered
